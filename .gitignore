# Secrets — never commit
.env
.env.local

# Cloned source-of-truth vault (not part of this project — managed separately)
parallax-obsidian/

# Render outputs and intermediates (regeneratable)
outputs/heygen_debug/
frames/
caption_ref_frames/
voice_auditions/

# Large media — recordings and renders (regeneratable; metadata sidecars stay tracked)
screen recordings/**/*.mp4
news-pipeline/recordings/
outputs/**/*.mp4
avatars/clips/*.mp4

# macOS cruft
.DS_Store
.~lock.*
~$*
*.tmp

# Python
__pycache__/
*.pyc

# Upstream skill source clone (regenerated by `npx skills update`; installed skills are sibling dirs)
.claude/skills/heygen-skills/
